const srcFolder = "./src";
const buildFolder = "./dist";

export const path = {
	build: {
		js: `${buildFolder}/`,
	},
	src: {
		js: `${srcFolder}/**/*.js`,
	},
	whatch: {
		js: `${srcFolder}/**/*.js`,
	}
}